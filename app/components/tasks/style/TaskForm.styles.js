// src/components/TaskForm.styles.js
import styled from 'styled-components';

export const ModalContainer = styled.div`
  @apply fixed inset-0 z-10 flex items-center justify-center bg-black bg-opacity-50;
`;

export const FormContainer = styled.div`
  @apply bg-white rounded-xl shadow-2xl max-w-md w-full p-8 transition-transform transform scale-105;
`;